{
    "builds": [
      {
        "src": "app.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)", 
        "dest": "/app.py/$1"
      }
    ],
    "env": {
    "PYTHONPATH": "src/"
}
  }